@inject Microsoft.AspNetCore.Identity.SignInManager<CustomIdentity.Data.ApplicationUser> signInManager
@if (signInManager.IsSignedIn(User))
{
    <form method="post" id="logoutForm" asp-action="logout" asp-controller="Account">
        @*<div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="userDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Hello, @User.Identity.Name!
            </button>
            <div class="dropdown-menu" style="border:none;padding:0px;" aria-labelledby="userDropdown">
                <a class="text-white nav-link btn btn-warning" asp-action="ChangePassword"
                   asp-controller="Account" asp-route-email="@User.Identity.Name">Change Password</a>
                <a class="text-white nav-link btn btn-danger" href="javascript:document.getElementById('logoutForm').submit()">Logout</a>
            </div>
           
        </div>*@
        <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="Permission" data-bs-toggle="dropdown" aria-expanded="false">
                @User.Identity.Name
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <li>
                    <a class="text-white nav-link btn btn-danger" href="javascript:document.getElementById('logoutForm').submit()">Logout</a>
                </li>
                <li>
                    <a class="text-white nav-link btn btn-warning" asp-action="ChangePassword"
                       asp-controller="Account" asp-route-email="@User.Identity.Name">Change Password</a>
                </li>
            </ul>
        </div>
    </form>
}
else
{
    <ul class="nav navbar-nav">
        <li class="nav-item">
            <a id="registerLink" class="nav-link" asp-action="Register" asp-controller="Account">Register</a>
        </li>
        <li><a id="loginLink" class="nav-link" asp-action="Login" asp-controller="Account">Login</a></li>
    </ul>
}